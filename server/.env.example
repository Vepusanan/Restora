# PRIVATE — never commit real values. Never expose via EXPO_PUBLIC_*.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-lite-latest

# Firebase web API key — used only to verify ID tokens (same as client public key).
FIREBASE_API_KEY=
FIREBASE_PROJECT_ID=

PORT=8787
